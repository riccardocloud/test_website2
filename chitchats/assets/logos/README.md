Drop company logos here as SVG (preferred) or transparent PNG. Monochrome versions work best against the dark-ink background of the logo strip.

Filename convention: `company-name.svg` (lowercase, hyphens, no punctuation).

The default `index.html` renders logo names as condensed type; swap to images by replacing the `.logo-item span` with an `<img>` tag.
